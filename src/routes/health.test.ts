describe('Health route', () => {
  const fakeApp = {
    get: jest.fn(),
  };

  beforeEach(() => jest.clearAllMocks());

  it('mounts with the correct endpoint', () => {
    throw new Error("todo");
  });

  it('responds with a JSON with the correct information', async () => {
    throw new Error("todo");
  });
});
